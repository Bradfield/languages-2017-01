const vm = require('../../vm')

// const flatten = arr => arr.reduce((a, b) => a.concat(Array.isArray(b) ? flatten(b) : b), [])

const bytes = (strs_and_ints) => bytes

vm.evalParsedObject({
  // other 
  code: bytes([
    'local_load', 2
  ])
})
